using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using DG.Tweening;

public class cooldownTimerTween : MonoBehaviour
{
    // Start is called before the first frame update

    // Update is called once per frame
    void CooldownDone()
    {
        // add tween to larger scale! transform.DOScale
    }
    void FadeMeOut()
    {
        //todo: add me :)
    }
}
